import { NextResponse } from "next/server";
import { requireRole, toErrorResponse } from "@/lib/auth/account";
export async function GET(_request: Request, { params }: { params: Promise<{ id: string }> }) { try { const ctx = await requireRole("viewer"); const { id } = await params; const { data, error } = await ctx.supabase.from("consulting_projects").select("*, consulting_artifacts(*), consulting_recommendations(*), consulting_action_items(*)").eq("account_id", ctx.accountId).eq("id", id).is("deleted_at", null).maybeSingle(); if (error) return NextResponse.json({ error: "Failed to load consulting project" }, { status: 500 }); if (!data) return NextResponse.json({ error: "Project not found" }, { status: 404 }); return NextResponse.json({ data }); } catch (error) { return toErrorResponse(error); } }
export async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> }) { try { const ctx = await requireRole("admin"); const { id } = await params; const body = await request.json().catch(() => null) as Record<string, unknown> | null; const updates: Record<string, unknown> = {}; for (const key of ["name", "objective", "status", "current_phase", "target_end_date"] as const) if (key in (body ?? {})) updates[key] = body?.[key]; if (typeof body?.scope === "string") updates.scope = { description: body.scope.trim() }; if (typeof updates.name === "string") updates.name = updates.name.trim(); if (Object.keys(updates).length === 0) return NextResponse.json({ error: "No valid fields to update" }, { status: 400 }); const { data, error } = await ctx.supabase.from("consulting_projects").update({ ...updates, updated_at: new Date().toISOString() }).eq("account_id", ctx.accountId).eq("id", id).is("deleted_at", null).select().single(); if (error) return NextResponse.json({ error: "Failed to update consulting project" }, { status: 500 }); return NextResponse.json({ data }); } catch (error) { return toErrorResponse(error); } }
export async function DELETE(_request: Request, { params }: { params: Promise<{ id: string }> }) { try { const ctx = await requireRole("admin"); const { id } = await params; const { data, error } = await ctx.supabase.from("consulting_projects").update({ deleted_at: new Date().toISOString(), status: "archived", updated_at: new Date().toISOString() }).eq("account_id", ctx.accountId).eq("id", id).is("deleted_at", null).select().single(); if (error) return NextResponse.json({ error: "Failed to archive consulting project" }, { status: 500 }); return NextResponse.json({ data }); } catch (error) { return toErrorResponse(error); } }
