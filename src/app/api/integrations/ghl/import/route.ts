import { NextResponse } from "next/server";
import { requireRole, toErrorResponse } from "@/lib/auth/account";
import { decryptGhlToken, fetchGhlContacts, getGhlConnection, hashPayload, normalizeGhlContact } from "@/lib/integrations/ghl";
export async function POST() { try { const ctx = await requireRole("admin"); const connection = await getGhlConnection(ctx.supabase, ctx.accountId); if (connection.error) return NextResponse.json({ error: "Failed to load GoHighLevel connection" }, { status: 500 }); if (!connection.data) return NextResponse.json({ error: "GoHighLevel is not configured" }, { status: 400 }); const secret = await ctx.supabase.from("ghl_connections").select("encrypted_access_token").eq("account_id", ctx.accountId).eq("id", connection.data.id).single(); if (secret.error || !secret.data?.encrypted_access_token) return NextResponse.json({ error: "GoHighLevel access token is missing" }, { status: 400 }); const { data: job, error: jobError } = await ctx.supabase.from("ghl_import_jobs").insert({ account_id: ctx.accountId, connection_id: connection.data.id, resources: ["contacts"], status: "running", started_at: new Date().toISOString(), created_by: ctx.userId }).select().single(); if (jobError) return NextResponse.json({ error: "Failed to start import job" }, { status: 500 }); try { const result = await fetchGhlContacts(decryptGhlToken(secret.data.encrypted_access_token), connection.data.location_id); let imported = 0; let skipped = 0; for (const contact of result.contacts) { if (!contact.id) continue; const normalized = normalizeGhlContact(contact); const existing = await ctx.supabase.from("ghl_import_records").select("id,local_id,payload_hash").eq("account_id", ctx.accountId).eq("resource_type", "contact").eq("external_id", contact.id).maybeSingle(); if (existing.data?.payload_hash === hashPayload(contact)) { skipped++; continue; } let localId = existing.data?.local_id ?? null; let match = normalized.email ? await ctx.supabase.from("contacts").select("id").eq("account_id", ctx.accountId).eq("email", normalized.email).limit(1).maybeSingle() : { data: null }; if (!match.data && normalized.phone) match = await ctx.supabase.from("contacts").select("id").eq("account_id", ctx.accountId).eq("phone", normalized.phone).limit(1).maybeSingle(); if (match.data?.id) localId = match.data.id; if (localId) await ctx.supabase.from("contacts").update({ ...normalized, updated_at: new Date().toISOString() }).eq("account_id", ctx.accountId).eq("id", localId); else { const created = await ctx.supabase.from("contacts").insert({ ...normalized, account_id: ctx.accountId, user_id: ctx.userId }).select("id").single(); if (created.error) throw created.error; localId = created.data.id; } await ctx.supabase.from("ghl_import_records").upsert({ account_id: ctx.accountId, job_id: job.id, resource_type: "contact", external_id: contact.id, local_id: localId, payload_hash: hashPayload(contact), status: "imported", updated_at: new Date().toISOString() }, { onConflict: "account_id,resource_type,external_id" }); imported++; } await ctx.supabase.from("ghl_import_jobs").update({ status: "completed", totals: { contacts: result.contacts.length, imported, skipped }, finished_at: new Date().toISOString() }).eq("id", job.id).eq("account_id", ctx.accountId); await ctx.supabase.from("ghl_connections").update({ status: "connected", last_import_at: new Date().toISOString(), updated_at: new Date().toISOString() }).eq("id", connection.data.id).eq("account_id", ctx.accountId); return NextResponse.json({ data: { job_id: job.id, contacts: result.contacts.length, imported, skipped, next_page: Boolean(result.nextPageUrl) } }); } catch (error) { await ctx.supabase.from("ghl_import_jobs").update({ status: "failed", error: error instanceof Error ? error.message : "Import failed", finished_at: new Date().toISOString() }).eq("id", job.id).eq("account_id", ctx.accountId); return NextResponse.json({ error: error instanceof Error ? error.message : "GoHighLevel import failed", job_id: job.id }, { status: 502 }); } } catch (error) { return toErrorResponse(error); } }
